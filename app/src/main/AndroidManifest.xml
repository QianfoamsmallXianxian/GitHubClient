<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- 旧版本才需要的存储读权限，Android 13+ 不再声明，避免系统启动时弹授权 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!--
      已移除：
        <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" ... />
      原因：
        1. 该权限在 Android 11+ 无法用 requestPermissions 申请，系统会持续在启动时提示用户去
           “设置 → 所有文件访问权限”，这正是“打开应用就提示授权存储空间”的来源。
        2. 本项目实际只需要“应用自己的目录”就能完成上传/下载/工具链解压。
           需要写入用户可见目录时，走 Storage Access Framework（SAF）即可，无需该权限。
      若日后确实需要，请在用户主动点击“选择下载目录”之类的按钮时，
      用 PermissionHelper.requestAllFilesAccess() 按需跳转，绝不放在启动流程里。
    -->

    <application
        android:name=".GitHubClientApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GitHubClient"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GitHubClient">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="oauth"
                    android:scheme="githubclient" />
            </intent-filter>
        </activity>

        <service
            android:name=".automation.UploadForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
